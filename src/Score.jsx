// import StudentCard from "./StudentCard";

const Score = (props) => {
  return ( 
    <div>
      <h1>Score</h1>
      {/* {props.stu.map(score =>
      <StudentCard score={score} />
    )} */}
    </div>
  );
}

export default Score;